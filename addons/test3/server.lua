local p = Ped(22, Player('morty').position)
local p2 = Ped(25, Player('morty').position)

-- addEventHandler('onPedWasted', resourceRoot, function()
--     iprint('wasted', source or 'nada', source.model)
-- end)